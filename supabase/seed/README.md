# Supabase Seed Files

Seed files used by local Supabase reset and controlled non-production setup.

Current seed:

- `cool_status.sql` — Cool status/config seed referenced by
  `supabase/config.toml`.
