# PWA Stub

This folder contains the retired Cloudflare Pages PWA stub. It is retained so
CI and deployment checks can fail closed with an explicit message instead of
silently deploying a stale PWA.

Do not add production UI here unless the PWA is formally revived with real
backend state, tests, security headers, and release ownership.
