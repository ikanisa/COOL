import 'package:flutter/material.dart';

import '../../shared/components/admin_page.dart';

class AdminPublicRequestsPage extends StatelessWidget {
  const AdminPublicRequestsPage({super.key});

  @override
  Widget build(BuildContext context) =>
      const AdminPage(title: 'Public requests');
}
