import 'package:flutter/material.dart';

import '../../shared/components/admin_page.dart';

class AdminPaymentEventsPage extends StatelessWidget {
  const AdminPaymentEventsPage({super.key});

  @override
  Widget build(BuildContext context) =>
      const AdminPage(title: 'Payment events');
}
