<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Production Android supports opt-in Rwanda MoMo receipt ingestion. The
         runtime remains disabled until the signed-in user gives consent. -->
    <application />
</manifest>
