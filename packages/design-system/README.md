# Design System Package

Reserved for extracted cross-surface design tokens and components.

Current source of truth remains `lib/core/theme`, `lib/shared/widgets`, and
app-local web CSS until contract tests prove shared extraction is safe.
