# API Clients Package

Reserved for shared typed clients around Supabase RPCs, Edge Functions, and
future service endpoints.

Current API clients remain app-local in `apps/admin/src/lib/api` and Flutter
feature repositories until cross-runtime contracts are proven.
