# Auth Package

Reserved for shared auth/session/role helper contracts.

Authorization must continue to be enforced by Supabase RLS, RPCs, and Edge
Functions. UI helpers here can only mirror backend policy for ergonomics.
