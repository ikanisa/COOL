# Testing Docs

Use this folder for test strategy, QA matrices, device integration guidance,
and CI gate documentation.

Current executable tests remain in `test`, `integration_test`, app-local
scripts, and `supabase/functions/**/*_test.ts`.
