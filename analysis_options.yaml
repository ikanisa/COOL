# COOL App — Analysis Options
# Strict rules for production-quality Flutter code.

include: package:flutter_lints/flutter.yaml

analyzer:
  exclude:
    # Vendored fork used by dependency_overrides; it is tracked for build
    # stability but should not inherit app-level strict lint gates.
    - apps/mobile/third_party/**
    - third_party/**
  errors:
    missing_return: error
    missing_required_param: error
  language:
    strict-casts: true
    strict-raw-types: true

linter:
  rules:
    # ── Safety ────────────────────────────────────────────────────
    avoid_print: true
    avoid_web_libraries_in_flutter: true
    cancel_subscriptions: true
    close_sinks: true
    literal_only_boolean_expressions: true
    no_adjacent_strings_in_list: true
    throw_in_finally: true
    unnecessary_statements: true

    # ── Style ─────────────────────────────────────────────────────
    prefer_const_constructors: true
    prefer_const_declarations: true
    prefer_const_literals_to_create_immutables: true
    prefer_final_fields: true
    prefer_final_in_for_each: true
    prefer_final_locals: true
    prefer_single_quotes: true
    sort_child_properties_last: true
    use_build_context_synchronously: true
    sized_box_for_whitespace: true
    use_colored_box: true
    use_decorated_box: true

    # ── Documentation & naming ────────────────────────────────────
    always_declare_return_types: true
    annotate_overrides: true
    avoid_renaming_method_parameters: true
    prefer_is_empty: true
    prefer_is_not_empty: true

    # ── Performance ───────────────────────────────────────────────
    avoid_unnecessary_containers: true
    prefer_if_elements_to_conditional_expressions: true
    use_key_in_widget_constructors: true
    avoid_slow_async_io: true
